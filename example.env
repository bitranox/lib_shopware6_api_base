# =============================================================================
# Shopware 6 API Configuration - Example
# =============================================================================
#
# Copy this file to .env and adjust the values for your shop.
# Values are loaded into ConfShopware6ApiBase.
#
# Documentation:
#   - Admin API: https://shopware.stoplight.io/docs/admin-api
#   - Store API: https://shopware.stoplight.io/docs/store-api
#   - Authentication: https://shopware.stoplight.io/docs/admin-api/ZG9jOjEwODA3NjQx-authentication-and-authorisation
#
# =============================================================================

# -----------------------------------------------------------------------------
# API Endpoints
# -----------------------------------------------------------------------------

# Admin API URL - used for administrative operations (CRUD on entities)
# Format: https://shop.yourdomain.com/api
SHOPWARE_ADMIN_API_URL="https://shop.example.com/api"

# Storefront API URL - used for storefront/customer-facing operations
# Format: https://shop.yourdomain.com/store-api
SHOPWARE_STOREFRONT_API_URL="https://shop.example.com/store-api"

# -----------------------------------------------------------------------------
# OAuth2 Security Settings
# -----------------------------------------------------------------------------

# Allow insecure transport (HTTP instead of HTTPS)
# WARNING: Set to "0" in production! Only use "1" for local development.
INSECURE_TRANSPORT="0"

# -----------------------------------------------------------------------------
# Authentication: User Credentials Grant Type
# -----------------------------------------------------------------------------
# Use this grant type for:
#   - Client applications performing administrative actions
#   - User-based authentication with refresh token support
#   - Interactive applications where a user logs in
#
# Setup: Admin Panel > Settings > System > Users
# URL: https://shop.example.com/admin#/sw/users/index

# Admin username
USERNAME="api-user@example.com"

# Admin password
PASSWORD="your-secure-password-here"

# -----------------------------------------------------------------------------
# Authentication: Resource Owner (Integration) Grant Type
# -----------------------------------------------------------------------------
# Use this grant type for:
#   - Machine-to-machine communications
#   - CLI jobs, cron tasks, and automated services
#   - Background workers and data synchronization
#   - No refresh token (request new token when expired)
#
# Setup: Admin Panel > Settings > System > Integrations
# URL: https://shop.example.com/admin#/sw/integration/index
#
# Create a new integration with appropriate permissions (e.g., "API Access")

# Integration Access ID (shown as "Access key ID" in admin panel)
CLIENT_ID="SWIAXXXXXXXXXXXXXXXXXXXX"

# Integration Secret Access Key (shown once when creating the integration)
CLIENT_SECRET="your-integration-secret-key-here"

# -----------------------------------------------------------------------------
# Grant Type Selection
# -----------------------------------------------------------------------------
# Which authentication method to use:
#
#   USER_CREDENTIALS (recommended for interactive apps):
#     - Uses USERNAME and PASSWORD
#     - Supports refresh tokens (longer sessions)
#     - Actions are attributed to the user account
#
#   RESOURCE_OWNER (recommended for automation):
#     - Uses CLIENT_ID and CLIENT_SECRET
#     - No refresh tokens (simpler token management)
#     - Actions are attributed to the integration
#
# Maps to GrantType enum in conf_shopware6_api_base_classes.py

GRANT_TYPE="RESOURCE_OWNER"

# -----------------------------------------------------------------------------
# Store API Authentication
# -----------------------------------------------------------------------------
# The sw-access-key for Storefront API requests.
# This key identifies your sales channel and controls API access.
#
# Setup: Admin Panel > Sales Channels > [Your Channel] > API Access
# URL: https://shop.example.com/admin#/sw/sales/channel/detail/{channel-id}
#
# The key is displayed in the "API Access" section of your sales channel.
# Format: SWSC... (26 characters)

STORE_API_SW_ACCESS_KEY="SWSCXXXXXXXXXXXXXXXXXX"
