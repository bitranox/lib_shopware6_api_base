# =============================================================================
# Shopware 6 API Configuration for Docker Test Container (dockware)
# =============================================================================
#
# This file configures the Shopware 6 API client for testing against a local
# dockware Docker container. Values are loaded into ConfShopware6ApiBase.
#
# Start the container with:
#   docker run -d --rm -p 80:80 --name dockware dockware/dev:latest
#
# Documentation:
#   - Dockware: https://developer.shopware.com/docs/guides/installation/dockware
#   - Admin API Auth: https://shopware.stoplight.io/docs/admin-api/ZG9jOjEwODA3NjQx-authentication-and-authorisation
#
# =============================================================================

# -----------------------------------------------------------------------------
# API Endpoints
# -----------------------------------------------------------------------------

# Admin API URL - used for administrative operations (CRUD on entities)
# Format: https://shop.yourdomain.com/api
SHOPWARE_ADMIN_API_URL="http://localhost/api"

# Storefront API URL - used for storefront/customer-facing operations
# Format: https://shop.yourdomain.com/store-api
SHOPWARE_STOREFRONT_API_URL="http://localhost/store-api"

# -----------------------------------------------------------------------------
# OAuth2 Security Settings
# -----------------------------------------------------------------------------

# Allow insecure transport (HTTP instead of HTTPS)
# Set to "1" for local development/testing only. Never use in production!
# Required for localhost testing without SSL certificates.
INSECURE_TRANSPORT="1"

# -----------------------------------------------------------------------------
# Authentication: User Credentials Grant Type
# -----------------------------------------------------------------------------
# Use this grant type for:
#   - Client applications performing administrative actions
#   - User-based authentication with refresh token support
#
# Setup: Admin Panel > Settings > System > Users
# URL: https://shop.yourdomain.com/admin#/sw/users/index

# Admin username (default dockware credentials)
USERNAME="admin"

# Admin password (default dockware credentials)
PASSWORD="shopware"

# -----------------------------------------------------------------------------
# Authentication: Resource Owner (Integration) Grant Type
# -----------------------------------------------------------------------------
# Use this grant type for:
#   - Machine-to-machine communications
#   - CLI jobs and automated services
#   - No refresh token (request new token when expired)
#
# Setup: Admin Panel > Settings > System > Integrations
# URL: https://shop.yourdomain.com/admin#/sw/integration/index

# Integration Access ID (client_id)
# Created automatically by _create_docker_test_container_resource_owner_credentials()
CLIENT_ID="SWIACWJOMUTXV1RMNGJUAKTUAA"

# Integration Secret Access Key (client_secret)
# Created automatically by _create_docker_test_container_resource_owner_credentials()
CLIENT_SECRET="UkhvUG1qdmpuMjFudGJCdG1Xc0xMbEt2ck9CQ2xDTUtXMUZHRUQ"

# -----------------------------------------------------------------------------
# Grant Type Selection
# -----------------------------------------------------------------------------
# Which authentication method to use:
#   - USER_CREDENTIALS: Uses username/password, supports refresh tokens
#   - RESOURCE_OWNER: Uses client_id/client_secret, no refresh tokens
#
# Maps to GrantType enum in conf_shopware6_api_base_classes.py
GRANT_TYPE="USER_CREDENTIALS"

# -----------------------------------------------------------------------------
# Store API Authentication
# -----------------------------------------------------------------------------
# The sw-access-key for Storefront API requests.
#
# Setup: Admin Panel > Sales Channels > [Channel] > API Access
# URL: https://shop.yourdomain.com/admin#/sw/sales/channel/detail/{id}
#
# Note: For dockware testing, this key is automatically retrieved from the
# container by the docker_test_config fixture and injected into the
# configuration at runtime. Leave empty for auto-detection.
STORE_API_SW_ACCESS_KEY=""
